<%= link_to "Пацьорки", root_path, :class => 'brand' %>
<ul class="nav">
  <%= content_tag :li, link_to(t('.products'), admin_products_path), :class => active_link_class('products') %>
  <%= content_tag :li, link_to(t('.categories'), admin_categories_path), :class => active_link_class('categories') %>
  <%= content_tag :li, link_to(t('.orders'), admin_orders_path), :class => active_link_class('orders') %>
  <%= content_tag :li, link_to(t('.pages'), admin_pages_path), :class => active_link_class('pages') %>
</ul>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    <%= link_to t(:logout), destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to t(:login), new_user_session_path %>
    </li>
  <% end %>

</ul>