<%= link_to "Пацьорки", root_path, :class => 'brand' %>
<ul class="nav">
  <%= content_tag :li, link_to("Товари", admin_items_path), :class => active_link_class('items') %>
  <%= content_tag :li, link_to("Категорії", admin_categories_path), :class => active_link_class('categories') %>
  <%= content_tag :li, link_to("Сторінки", admin_pages_path), :class => active_link_class('pages') %>
</ul>
<ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    <%= link_to t(:logout), destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to t(:login), new_user_session_path %>
    </li>
  <% end %>

</ul>